# 用 Quartz 把 Obsidian 笔记发布成免费静态网站(GitHub Pages)

> 站点源码已经在本机准备好了:`D:\蓝色大肥鱼\quartz-site`
> 笔记来源:`D:\蓝色大肥鱼\学习`(Obsidian 库根目录)
> 本地预览已验证通过:10 篇 Markdown → `public\` 里 103 个文件。

技术栈:**Quartz v5.0.0**(2026 年最新版,配置是 YAML,和网上大量 v4 教程不同)
费用:**0 元**。GitHub Pages 对公开仓库免费,不限流量、无需信用卡、无订阅。

---

## 0. 需要你先做的一件事:装 Git

Quartz 的发布流程依赖 Git。下载安装包(选 64 位 Windows Standalone Installer):

- 官网下载页:<https://git-scm.com/download/win>
- 直接下载链接(会随版本更新):<https://git-scm.com/download/win/win>

安装时**保持默认**即可,只要注意这一项:

> 选择 **"Git from the command line and also from 3rd-party software"**(默认就是它)

装完**关掉并重新打开** PowerShell,输入 `git -v` 能打印版本号就成功了。

Node.js 不用装:你机器上已有 `node v24.21.0` / `npm 11.19.0`,满足 Quartz 要求的 Node ≥ 22、npm ≥ 10.9.2。

---

## 1. 注册 GitHub 并建仓库

1. 注册:<https://github.com/signup>(免费,邮箱即可)
2. 建仓库:<https://github.com/new>
   - **Repository name** 填 `notes`(URL 会变成 `https://<你的用户名>.github.io/notes`)
   - 选 **Public**(私有仓库的 Pages 要付费,免费只能用公开仓库)
   - ⚠️ **不要**勾选 "Add a README file"、".gitignore"、"license" —— 留空,否则第一次 push 会冲突
3. 建好后页面上会显示仓库地址,形如 `https://github.com/<你的用户名>/notes.git`,记下来。

## 2. 把 `baseUrl` 改成你自己的地址

打开 `D:\蓝色大肥鱼\quartz-site\quartz.config.yaml`,把第 ~12 行改成:

```yaml
  baseUrl: <你的用户名>.github.io/notes
```

不要写 `https://`,不要写结尾的 `/`。
这一项决定 sitemap、RSS、分享卡片里的绝对链接,现在里面还是占位符 `yourname.github.io/notes`,**必须改**,否则搜索引擎和社交分享会指向不存在的域名。

顺便可以改的几项(同一文件顶部):

| 字段 | 现值 | 说明 |
|---|---|---|
| `pageTitle` | 蓝色大肥鱼 Notes | 站点标题 |
| `locale` | zh-CN | 界面语言(搜索框、日期格式) |
| `analytics` | null | 想加统计再填,例如 `{ provider: plausible }` |
| `footer.links` | GitHub 占位链接 | 建议换成你的 GitHub 主页 |

## 3. 先本地预览一次(可选但推荐)

在 `quartz-site` 目录打开 PowerShell:

```powershell
cd 'D:\蓝色大肥鱼\quartz-site'
.\sync-and-preview.ps1
```

浏览器打开 <http://localhost:8080>。这个脚本会:

1. 用 robocopy 把 `学习\` 的笔记同步到 `quartz-site\content\`(排除 `.obsidian` 和 Obsidian 自带的 `欢迎.md`)
2. 从 `site-index.md` 生成站点首页 `content\index.md`
3. 构建并起本地服务器

只构建不起服务器:`.\sync-and-preview.ps1 -BuildOnly`

## 4. 初始化 Git 仓库并推送

在 `quartz-site` 目录里依次执行(把 `<用户名>` 换成你的):

```powershell
cd 'D:\蓝色大肥鱼\quartz-site'

git init -b v5
git add -A
git commit -m "init: Quartz 5 site with GEST1014 notes"

git remote add origin https://github.com/<用户名>/notes.git
git push -u origin v5
```

> 为什么分支叫 `v5`?Quartz 5 的官方 GitHub Pages 部署工作流默认监听 `v5` 分支,
> 我们本地的 `.github\workflows\deploy.yml` 也是这样写的。分支名保持一致即可。

推送时如果弹出登录窗口,选 **"Sign in with your browser"**,用 GitHub 账号授权一次就行。

## 5. 打开 GitHub Pages

1. 打开 `https://github.com/<用户名>/notes` → **Settings**
2. 左侧 **Pages** → **Source** 选 **GitHub Actions**(不要选 "Deploy from a branch")
3. 回到仓库的 **Actions** 标签,能看到 `Deploy Quartz site to GitHub Pages` 正在跑(约 1–2 分钟)
4. 跑完后访问:

```
https://<用户名>.github.io/notes
```

## 6. 以后每次更新笔记

笔记改完后,只要两条命令:

```powershell
cd 'D:\蓝色大肥鱼\quartz-site'
.\sync-and-preview.ps1 -BuildOnly    # 同步 + 本地构建(想先预览就不加 -BuildOnly)
git add -A
git commit -m "update notes"
git push
```

push 之后 GitHub Actions 会自动重新构建并发布,约 1 分钟后线上就更新了。

---

## 常见问题

**Q: 为什么不是 `content` 里直接放笔记,而要同步?**
你的 Obsidian 库根目录是 `D:\蓝色大肥鱼\学习`,Quartz 源码在 `D:\蓝色大肥鱼\quartz-site`。两者分开的好处是:发布配置(node_modules、构建产物)不会污染笔记库,Obsidian 也不会被站点文件干扰。同步是一次 robocopy,秒级完成。

**Q: 想让某个笔记不被公开?**
在它的 frontmatter 里加 `draft: true` —— Quartz 的 remove-draft 插件会跳过它。
或者在 `quartz.config.yaml` 的 `ignorePatterns` 里加目录名。

**Q: Node 构建报 `EPERM: spawn`?**
那是本会话沙箱对子进程的限制,你自己在 PowerShell 里跑不会有这个问题。

**Q: 部署报 "not allowed to deploy to github-pages due to environment protection rules"?**
去 Settings → Environments,把里面已存在的 `github-pages` 环境删掉,下次部署会自动重建。

**Q: 想换配色/主题?**
改 `quartz.config.yaml` 里 `theme.colors.lightMode` / `darkMode` 的十六进制颜色即可,
改完本地 `.\sync-and-preview.ps1` 预览,满意再 push。

**Q: 想用自定义域名?**
Settings → Pages → Custom Domain 填写域名,再按 GitHub 文档加 DNS 记录(A 记录或 CNAME)。
之后把 `baseUrl` 改成你的域名并重新 push。

---

## 参考链接

| 用途 | 链接 |
|---|---|
| Git for Windows 下载 | <https://git-scm.com/download/win> |
| GitHub 注册 | <https://github.com/signup> |
| 新建仓库 | <https://github.com/new> |
| Quartz 项目主页 | <https://github.com/jackyzha0/quartz> |
| Quartz 官方文档 | <https://quartz.jzhao.xyz/> |
| 官方 GitHub Pages 部署教程 | <https://quartz.jzhao.xyz/hosting#github-pages> |
| 配置项参考 | <https://quartz.jzhao.xyz/configuration> |
